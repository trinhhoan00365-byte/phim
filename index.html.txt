<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Xem Phim Online - MovieZone</title>
  <style>
    * { box-sizing: border-box; }
    body {
      margin: 0;
      font-family: 'Arial', sans-serif;
      background-color: #0a0a0a;
      color: #fff;
    }
    header {
      background-color: #111;
      padding: 15px 25px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      position: sticky;
      top: 0;
      z-index: 100;
    }
    header h1 {
      color: #e50914;
      margin: 0;
      font-size: 24px;
    }
    #search {
      padding: 8px 10px;
      font-size: 14px;
      border-radius: 5px;
      border: none;
      outline: none;
    }
    .movies {
      display: flex;
      flex-wrap: wrap;
      gap: 20px;
      justify-content: center;
      padding: 20px;
    }
    .movie {
      width: 180px;
      background-color: #1a1a1a;
      border-radius: 10px;
      overflow: hidden;
      cursor: pointer;
      transition: transform 0.3s;
    }
    .movie:hover {
      transform: scale(1.07);
    }
    .movie img {
      width: 100%;
      height: 250px;
      object-fit: cover;
    }
    .movie h3 {
      margin: 10px;
      font-size: 16px;
      color: #fff;
      text-align: center;
    }
    #player {
      display: none;
      text-align: center;
      padding: 20px;
    }
    #player video {
      width: 90%;
      max-width: 900px;
      border-radius: 10px;
      border: 2px solid #333;
    }
    #back-btn {
      background-color: #e50914;
      border: none;
      padding: 8px 15px;
      border-radius: 5px;
      color: white;
      font-weight: bold;
      cursor: pointer;
      margin-bottom: 15px;
    }
    #no-result {
      text-align: center;
      padding: 20px;
      color: #999;
    }
  </style>
</head>
<body>
  <header>
    <h1>?? MovieZone</h1>
    <input id="search" type="text" placeholder="Tìm phim...">
  </header>

  <div class="movies" id="movie-list"></div>
  <div id="no-result" style="display:none;">Không tìm th?y phim nào.</div>

  <div id="player">
    <button id="back-btn">? Tr? l?i</button>
    <h2 id="movie-title"></h2>
    <video id="video-player" controls></video>
  </div>

  <script>
    const movies = [
      {
        title: "Phim Demo 1",
        image: "https://i.imgur.com/N3vQFzX.jpeg",
        src: "https://www.w3schools.com/html/mov_bbb.mp4"
      },
      {
        title: "Phim Demo 2",
        image: "https://i.imgur.com/Z6DURGl.jpeg",
        src: "https://www.w3schools.com/html/movie.mp4"
      },
      {
        title: "Phim Demo 3",
        image: "https://i.imgur.com/9z8YcR8.jpeg",
        src: "https://www.w3schools.com/html/mov_bbb.mp4"
      },
      {
        title: "Phim Demo 4",
        image: "https://i.imgur.com/x5kY2Cl.jpeg",
        src: "https://www.w3schools.com/html/movie.mp4"
      }
    ];
const movieList = document.getElementById("movie-list");
    const player = document.getElementById("player");
    const video = document.getElementById("video-player");
    const movieTitle = document.getElementById("movie-title");
    const searchBox = document.getElementById("search");
    const noResult = document.getElementById("no-result");

    function renderMovies(list) {
      movieList.innerHTML = "";
      if (list.length === 0) {
        noResult.style.display = "block";
      } else {
        noResult.style.display = "none";
        list.forEach((m, i) => {
          const div = document.createElement("div");
          div.className = "movie";
          div.innerHTML = `
            <img src="${m.image}" alt="${m.title}">
            <h3>${m.title}</h3>
          `;
          div.onclick = () => playMovie(m);
          movieList.appendChild(div);
        });
      }
    }

    function playMovie(movie) {
      player.style.display = "block";
      movieList.style.display = "none";
      noResult.style.display = "none";
      movieTitle.textContent = movie.title;
      video.src = movie.src;
      window.scrollTo({ top: 0, behavior: "smooth" });
    }

    document.getElementById("back-btn").onclick = () => {
      player.style.display = "none";
      movieList.style.display = "flex";
    };

    searchBox.addEventListener("input", e => {
      const keyword = e.target.value.toLowerCase();
      const filtered = movies.filter(m => m.title.toLowerCase().includes(keyword));
      renderMovies(filtered);
    });

    renderMovies(movies);
  </script>
</body>
</html>
